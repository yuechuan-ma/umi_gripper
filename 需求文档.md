硬件：Waveshare Bus Servo Adapter (A)、Waveshare ST3215、飞特STS3215、USB-C转接线、12V电源、7.4V电源

功能描述：上位机（Linux/Windows）直接通过USB-C连接驱动板，驱动板控制舵机，舵机最终会通过传动机构控制夹爪

具体需求：

1. 所有设计需兼容ST3215及STS3215舵机
2. 代码可在Linux或Windows系统上运行（自动匹配）
3. 配置文件记录超参数，包括中立位置、张开位置、闭合位置（描述是按夹爪描述，但最终都对应到舵机的位置上）、运动速度、夹紧力度、串口、舵机ID。运动速度和夹紧力度根据两个舵机的实际参数给一个预设值，其他部分置空。实际执行时，每次都必须检查超参是否完整并且在合理范围内（如张开位置不能大于舵机最大位置等），如果不完整或不合理则报错。
4. gripper_init.py：首先确认串口和舵机ID（自动匹配操作系统、自动匹配可能的串口和舵机ID），如果有多个可选项，在终端供用户选择或提示用户退出当前进程，确认后再次init。之后标定中立位置、张开位置和闭合位置，具体步骤是：关闭扭矩，终端输出提示用户将夹爪移到中立位置，按回车确认，即读取当前位置并填入配置文件，张开和闭合位置同理。
5. gripper类：主要实现.goto()方法，取值范围为0到1；以及get_width()方法，返回0到1，即当前位置；reset()方法，返回中立位置。取值范围是0到1的原因是，以张开位置为1，闭合位置为0换算。注意goto时是非等待执行，目标位置下发给舵机后直接返回，而reset时是等待执行，等待舵机回到中立位置后才返回。也可以有get_state()方法返回其他信息，这根据具体实现中的需要而定。
6. 夹紧力度和运动速度根据舵机参数合理预设，要有“没有移动到目标位置但是已经夹紧物体”的判断（通过读取负载和电流判断），夹紧物体后保持夹紧力度，但不会继续增加力度。
7. 所有设计均要考虑异常处理情况，报错要清晰。但是代码不能太过冗余。
8. 项目文件夹位置："E:\backup\RAYear\Internship\StellarEdgeAI\umi\software\gripper"，我希望你只生成五个文件：gripper_init.py、gripper类文件、配置文件、requirement.txt以及quickstart.py（输入0或1，使夹爪移动到位，能处理夹紧物体的情况，即调用goto方法）
9. 告诉我用哪个python版本，我来创建虚拟环境然后告诉你。